# 为什么换了策略，NPU 平均利用率几乎不动

> 写给第一次看这个结论的人。全文不用任何超出小学算术的东西，每一步都可以自己拿计算器验证。
>
> 相关正式报告：`DEMAND_AWARE_SCHEME_B_DEPLOYMENT_REPORT.pdf` 第 2 章。

---

## 0. 你的疑问是什么

你问的是这个：

> 平均利用率不就是「把每个 NPU 的利用率算出来，然后求平均」吗？
> 为什么可以扯到「每 GB 数据换 48 ms 计算」上去？

答案是：**这两件事算出来是同一个数字。** 不是我偷换了指标，是同一个东西的两种写法。

下面分三步走，每一步都很简单，难的是把三步串起来。

---

## 第一步：「先算比值再平均」和「先求和再算比值」是同一个数

### 先看一个考试的例子

两个学生考同一张卷子，**满分都是 100 分**：

- 小明考了 60 分
- 小红考了 20 分

平均分怎么算？两种算法：

**算法 A（先算比值再平均）**

```
  小明得分率 = 60/100 = 60%
  小红得分率 = 20/100 = 20%
  平均       = (60% + 20%) / 2 = 40%
```

**算法 B（先求和再算比值）**

```
  总得分   = 60 + 20 = 80 分
  总满分   = 100 + 100 = 200 分
  得分率   = 80/200 = 40%
```

**两个算法结果一样，都是 40%。**

为什么一样？因为**两个人的满分相同**（都是 100）。

如果小明考的是 100 分制、小红考的是 150 分制，这两种算法就**不一样**了，不能互换。

### 换成 NPU

现在把「学生」换成「NPU」，「得分」换成「实际计算了多少毫秒」，「满分」换成「测量窗有多长」。

假设测量窗 T = 100 ms，有 2 个 NPU：

```
  NPU-A 实际计算了 60 ms   ->  利用率 60/100 = 60%
  NPU-B 实际计算了 20 ms   ->  利用率 20/100 = 20%

  算法 A：平均 = (60% + 20%) / 2 = 40%
  算法 B：总计算 80 ms / (2 个 NPU × 100 ms) = 80/200 = 40%
```

**一样。** 因为所有 NPU 用的是**同一个测量窗 T**，就像所有学生考同一张卷子。

### 写成公式

```
                   1      c_i          c_1 + c_2 + ... + c_N        总计算时间
   平均利用率  =  ---  * SUM ---   =   ----------------------  =  ----------------
                   N      i    T                N * T              NPU数 * 窗口长
```

- `c_i` = 第 i 个 NPU 在窗内实际计算的毫秒数
- `T`   = 测量窗长度（所有 NPU 共用同一个）
- `N`   = NPU 数量

### 代码里确实是这样

`continuous_batch_sim.py:2053`：

```python
npu_utilizations = [busy_ms / duration_ms for busy_ms in compute_ms_by_npu]
```

`duration_ms` 是**一个标量**，不是每个 NPU 一个。所以所有 NPU 除的是同一个数。

### 用真实数据验证过

我把全部 32 次正式运行都算了一遍，比较两种算法：

```
  max | mean(npu_utilizations)  -  sum(compute_ms)/(N*T) |  =  3.3e-16
```

`3.3e-16` 就是零（浮点数的舍入噪声）。**两种算法完全相同。**

> ### 第一步小结
>
> 你说的「每个 NPU 各算各的再平均」是对的。
> 而它**恒等于**「全队总计算时间 ÷ 全队总可用时间」。
> 所以要研究平均利用率，只需要研究一件事：**全队一共算了多少毫秒。**

---

## 第二步：全队一共算了多少毫秒，被「送了多少数据」锁死

### 直觉

NPU 是工人，SSD 是给工人送原料的传送带。

**工人没有原料就不能干活。** 在这个 workload 里，关系是固定的：

```
   送 1 GB 数据   ->   换来约 48 ms 的计算
```

所以：

```
   全队总计算时间(ms)  =  48  *  全队收到的总数据量(GB)
```

这就是把「计算时间」翻译成「数据量」的汇率。

### 这个 48 不是理所当然的

**单看一个请求，这个数完全不成立。** 项目里有四类请求，它们的「每 GB 换多少 ms」差 6 倍：

| 类别 | 每层字节 | 每层计算 | **每 GB 换多少 ms** |
|---|---:|---:|---:|
| SS | 0.107338 GB | 1.288 ms | **12.00** |
| LS | 0.193024 GB | 6.695 ms | **34.69** |
| LL | 0.192688 GB | 13.097 ms | **67.97** |
| SL | 0.106750 GB | 7.729 ms | **72.40** |

SS 类每 GB 只换 12 ms，SL 类每 GB 换 72.40 ms。差 6 倍。

**那为什么全队合起来又是一个固定的 48？**

### 因为工作负载是刻意配平的

我从代码里把每个 NPU 拿到的请求组成查了出来：

```
  per-NPU category mix distinct patterns: 1  ->  (LL:8, LS:8, SL:8, SS:8)
  per-NPU ms/GB      : min = 48.0310   max = 48.0310    spread = 0.0000%
  per-NPU raw demand : min = 20.8199   max = 20.8199 GB/s
```

**每个 NPU 都拿到 8 个 SS + 8 个 SL + 8 个 LS + 8 个 LL，一模一样。**

`spread = 0.0000%` 表示 128 个 NPU 的「每 GB 换多少 ms」**一个小数点都不差**。

手算验证（8 个一组，每类各 8 个）：

```
        8 * (1.288 + 6.695 + 13.097 + 7.729)      230.47 ms
   48 = ------------------------------------- = ------------- = 48.03 ms/GB
        8 * (0.1073 + 0.1930 + 0.1927 + 0.1068)     4.798 GB
```

分子是一个 NPU 所有请求的总计算时间，分母是总字节。得到 **48.03 ms/GB**。

### 这个数和报告里的 D = 2664.945 是同一件事

报告里有个数叫 fleet raw demand `D = 2664.945 GB/s`，意思是「要让所有 NPU 一刻不停，盘侧总共要送多快」。

它就是 48.03 的倒数换个写法：

```
   一个 NPU 一刻不停需要：  1000 ms / 48.031 ms/GB  =  20.8199 GB/s
   128 个 NPU 合计：        128 * 20.8199           =  2664.945 GB/s   <- 与报告完全一致
```

> **所以「48.0 ms/GB」和「D = 2664.945」不是两条独立证据，是同一个常数的两种写法。**
> 报告里那个 insightbox 写「等价说法」是对的，但没交代等价的来源是**每个 NPU 类别均衡**。
> 这一点是你没看懂的主要原因，责任在报告没写清楚。

---

## 第三步：送了多少数据，由盘决定

一块盘在忙的时候固定跑 40 GB/s（这个仿真里一块盘同时只跑一条命令）。所以：

```
   一块盘送出的数据 = 40 GB/s * 它忙碌的时间
   全部盘送出的数据 = 盘数 * 40 GB/s * 平均忙碌比例 * 窗口时长
```

「平均忙碌比例」就是报告里的 `u_ssd`。

以 SSU24 baseline 为例：

```
   24 块盘 * 40 GB/s * 0.9094(忙碌比例) * 2 秒  =  1746 GB
```

实测 `sum(measurement_ssd_served_gb_by_ssu) = 1746.1 GB`。对上了。

---

## 把三步串起来

```
              总计算时间           48 * 总数据量           48 * 盘数 * 40 * u_ssd * 时长/1000
  平均利用率 = ------------  =  -------------------  =  ----------------------------------
              NPU数 * 时长        NPU数 * 时长                    NPU数 * 时长


                 盘数 * 40 * u_ssd
             =  -------------------
                        D


  三个等号分别靠：
    (1) 所有 NPU 共用同一个 T      <- 第一步
    (2) 每 GB 换 48 ms 是常数       <- 第二步
    (3) 盘忙的时候固定 40 GB/s      <- 第三步
```

### 手算一遍 SSU24 baseline

```
   24 * 40 * 0.9094 / 2664.945  =  873.02 / 2664.945  =  0.3276  =  32.76%
```

实测 `mean_npu_utilization = 0.32763`。**对上了。**

---

## 所以策略为什么提不动利用率

看这条链：

```
   策略能改的：谁先拿到数据
                    |
                    v
   策略改不了：盘一共送出多少数据  <- 由盘的忙碌比例决定
                    |
                    v
   于是改不了：全队总计算时间
                    |
                    v
   于是改不了：平均利用率
```

**策略在「分蛋糕」，不是在「做蛋糕」。**

### 真实数据：分蛋糕的动作非常大

同样是 128 NPU × 24 SSU：

| | 平均利用率 | 最低 NPU | 最高 NPU | 标准差 | **SLO** |
|---|---:|---:|---:|---:|---:|
| baseline | 32.76% | 0.3021 | 0.3690 | 0.0248 | **25.00%** |
| adaptive | 32.51% | **0.1666** | **0.5040** | **0.0920** | **53.88%** |

- **平均值几乎没动**（32.76% → 32.51%）
- **标准差涨了 3.7 倍**（0.0248 → 0.0920）
- **最高最低差距从 1.22 倍拉到 3.03 倍**
- **SLO 翻了一倍多**（25.00% → 53.88%）

策略把一部分 NPU 从 0.30 提到 0.50，就必然把另一部分从 0.30 压到 0.17。
**总和是固定的，因为每 GB 只能换 48 ms 计算，谁拿到都一样。**

这就是「零和」的严格含义。

### 但这不代表策略没用

在 PD 分离的 P 阶段，**错过 TTFT 的请求等于白算**。所以该看的是「有效算力 = 利用率 × SLO」：

| SSU | baseline | adaptive | 提升 |
|---:|---|---|---:|
| 24 | 32.76% × 25.00% = **8.19%** | 32.51% × 53.88% = **17.52%** | **2.14 倍** |
| 40 | 51.60% × 50.00% = **25.80%** | 51.57% × 82.87% = **42.73%** | **1.66 倍** |
| 70 | 88.93% × 77.41% = **68.84%** | 90.85% × 98.89% = **89.84%** | **1.31 倍** |

**平均利用率是个被瓶颈钉死的吞吐指标，对「按需分配」结构性不敏感。它不适合用来衡量这个策略。**

---

## 常见误解

### Q1：那 48 是我瞎凑的吗？

不是。它由工作负载唯一确定：每个 NPU 8/8/8/8 的类别配比 + 四类的字节/计算参数。
换一个工作负载，这个常数会变成别的值，但**在同一个工作负载内它是常数**，与策略无关。

### Q2：实测是 47.5711 ~ 48.4227，不是恰好 48.031，差的这 1.77% 是什么？

是**窗内配比偏移**，不是策略在改变物理。

SSU24 时窗内只完成 768/128 = **每个 NPU 6 个请求**。6 个请求装不下 4 个类别的均衡配比（6÷4 = 1.5），所以窗内的实际配比会偏离 8/8/8/8。再加上窗口首尾有跨界的在途工作。

关键证据：这个偏差**在 10 种策略之间没有系统性方向**，均值 48.0375、标准差 0.1867，全域离散度仅 1.77%。

### Q3：盘的忙碌比例 u_ssd 是 0.9094，还有 9% 空闲，调度调好点不能榨出来吗？

不能。我查过每块盘：

```
   SSU24 baseline：最热的盘忙碌率 = 1.0000（已经打满）
                   最冷的盘忙碌率 = 0.8189
                   每块盘的忙碌率与它拥有的数据量相关系数 = 0.9975
```

**那 9% 空闲全部在冷盘上，而且是结构性的**：冷盘停下来不是因为「排队策略让它等」，而是因为这一层它该读的字节已经读完了，剩下的字节**不在它身上**。

调度能改变「盘上有多个请求排队时先服务谁」，改变不了「队列是空的」。要动这部分只能改 placement（把字节挪到别的盘上）。

### Q4：所以我的程序错了吗？

**没有错。** 这是这类调度器的真实性质，不是 bug。

工作保持（work-conserving）的比例分享调度器（NVMe WRR、Linux BFQ / io.cost）本来就有这个性质：多余的带宽总会被立刻分给其他有积压的流，所以带宽重分配对总吞吐是零和的。

要让「盘侧不知道需求」这件事本身压低利用率，需要**非工作保持的硬预留**（NVM Sets、SR-IOV token bucket、Linux blk-throttle io.max、云盘 provisioned IOPS）。当前仿真处在纯工作保持这一端。

---

## 一页速查

```
  平均利用率 = 各 NPU 利用率的平均            <- 你原本的理解，正确
             = 总计算时间 / (NPU数 * 窗口长)   <- 恒等，因为共用同一个 T
             = 48 * 总数据量 / (NPU数 * 窗口长) <- 每 GB 换 48 ms（类别均衡）
             = 盘数 * 40 * u_ssd / 2664.945    <- 盘忙时固定 40 GB/s

  结论：利用率 = 吞吐的线性像，没有独立自由度。
        策略只能重新分配，不能创造。
        收益在 SLO，不在利用率。
```

---

## 证据出处

| 说法 | 来源 |
|---|---|
| 两种算法恒等（差 3.3e-16） | 全部 32 次运行的 `results/*/results.json` |
| 每 NPU 类别均衡 8/8/8/8 | `steady_state_workload.py` + 真实 `data` 请求画像 |
| 每 NPU ms/GB 离散度 0.0000% | 同上 |
| 48.0375 ± 0.1867（32 次运行） | `compute_ms_by_npu` 与 `measurement_ssd_served_gb_by_ssu` |
| 盘忙时恒 40 GB/s（偏差 7.1e-15） | `continuous_batch_sim.py:1944` |
| 忙碌率与 ownership 相关 0.9975 | `measurement_ssd_utilizations` vs ring placement 需求 |
| 利用率/SLO 对照 | `results/steady_state_128npu_admission_ssu24_pair/`、`results/steady_state_128npu_adaptive_v2_1/` |
