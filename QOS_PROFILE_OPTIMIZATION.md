# QoS 静态 CIR 优化结果

## 1. 结论

当前 `(20, 6, 8, 6)` 四类方案有明显优化空间。最终建议不是把 Path 数偏向某一类，
而是使用 **8 个类别、每类每 Group 固定 4 条 Path**，再根据已安装 SSU 数选择
经过验证的静态 profile：

| SSU 数 | 使用的 profile | 说明 |
|---:|---|---|
| 8、16 | `OPTIMIZED_LOW_SSU_8Q` | 强争用，主要按每层 KV 数据量区分短、长请求 |
| 28、56、80、112 | `OPTIMIZED_HIGH_SSU_8Q` | 加入较轻的请求带宽权重 |
| 40 | `CURRENT_STATIC` | 保守回退；两个留出 seed 上新 profile 有小幅回退 |

代码入口是 `strategy_profiles.recommended_static_profile(num_ssu)`。它只接受上表中
实际验证过的 SSU 数，避免把离散搜索结果随意外推到未测试硬件规模。

最终未参与调参的 seed=48/49 盲测结果：

| 策略 | 平均请求计算占比 | Fleet 计算利用率 | Jain 公平性 | Makespan | 胜/平/负 |
|---|---:|---:|---:|---:|---:|
| 当前四类 | 72.633% | 15.446% | 0.8576 | 2047.43 ms | — |
| 最终保守方案 | **75.616%** | **15.451%** | **0.8667** | **2047.24 ms** | **12/2/0** |
| 非均匀 Path 仿真候选 | 75.999% | 15.395% | 0.8709 | 2054.90 ms | 14/0/0 |

最终方案比当前高 **2.983 个百分点**；Fleet、Makespan 没有恶化。非均匀 Path
候选的主指标只再高 0.383 个百分点，却依赖当前 Path 级调度的服务票数效应，
因此没有把它作为硬件推荐。

## 2. 请求如何进入 8 个类别

客户端为每个请求计算：

```text
score = per_layer_kv_gb / min(required_bw_input_gbps, 50)^alpha
```

`score` 越小，越接近短作业，进入越靠前的 Q0、Q1……类别。边界由正式请求画像
的等概率分桶产生，不依赖实验随机 seed。

- 低 SSU profile：`alpha = 0`，只看每层 KV 数据量。
- 高 SSU profile：`alpha = 0.2`，对带宽需求增加很轻的修正。

这 8 类是新的 **QoS 路由类别**，不是把原 SS/SL/LS/LL 指标标签改名。结果中会
同时保留原 `category` 和新的 `qos_routing_class`，因此仍可按原四类统计。

## 3. CIR 和 Path 如何配置

每个 Group 的 32 条 Path 均匀分配：Q0 用组内偏移 0~3，Q1 用 4~7，……，
Q7 用 28~31。8 个 Group 合计后，每类有 32 条 Path，总数仍为 256。

当前仿真把类别 CIR 均分给该类的 32 条 Path，即：

```text
每条 Path CIR = 类别 CIR / 32
```

### 低 SSU：8、16

分桶依据是 `per_layer_kv_gb`，边界依次为：

```text
0.050983, 0.072468, 0.093952, 0.115437,
0.158405, 0.201374, 0.244343 GB
```

| 类别 | 聚合 CIR (GB/s) | 每 Path CIR (GB/s) |
|---|---:|---:|
| Q0 | 25.045983 | 0.782687 |
| Q1 | 7.510524 | 0.234704 |
| Q2 | 3.295016 | 0.102969 |
| Q3 | 1.803548 | 0.056361 |
| Q4 | 0.989727 | 0.030929 |
| Q5 | 0.576184 | 0.018006 |
| Q6 | 0.421551 | 0.013173 |
| Q7 | 0.357468 | 0.011171 |
| **合计** | **40.000000** | — |

所有类别都有正 CIR；没有采用主指标略高但会让尾类 CIR 为零的极端候选。

### 高 SSU：28、56、80、112

`score` 的边界依次为：

```text
0.031576, 0.044176, 0.056672, 0.072641,
0.095069, 0.116207, 0.144982
```

| 类别 | 聚合 CIR (GB/s) | 每 Path CIR (GB/s) |
|---|---:|---:|
| Q0 | 17.506477 | 0.547077 |
| Q1 | 8.288639 | 0.259020 |
| Q2 | 5.238940 | 0.163717 |
| Q3 | 3.176398 | 0.099262 |
| Q4 | 2.215386 | 0.069231 |
| Q5 | 1.484625 | 0.046395 |
| Q6 | 1.213918 | 0.037935 |
| Q7 | 0.875617 | 0.027363 |
| **合计** | **40.000000** | — |

40 SSU 继续使用原四类 `(20, 6, 8, 6)` 和每 Group Path 数 `(12, 4, 12, 4)`。

## 4. 搜索和验证方法

数据面始终不变：128 NPU、16 层、SSD 40 GB/s、NPU 接收链路 50 GB/s、
refresh8、batch 1、0.1 us 发命令间隔，以及原始不可变 block placement。

搜索程序共生成 54 个静态候选：

- 5 个四类候选；
- 45 个八类候选，搜索 `alpha`、CIR 曲线陡峭度、尾类 CIR 下限和 Path 布局；
- 4 个十六类候选。

十六类并没有更好。每类每 Group 只剩 2 条 Path 后，量化更细带来的收益不足以
抵消 Path 选择空间变小。类别越多并不必然越优；在这套 32 Path/Group 硬件上，
8 类正好对应每类 4 Path，是更好的折中。

验证过程刻意分阶段：seed=42 用于搜索，43 用于首轮验证；44/45、46/47 暴露并
修正了只按一个阈值切换的问题；最终方案冻结后，才在未使用的 seed=48/49 上做
最后盲测。只有 seed=48/49 的结果应视为最终方案的无偏留出结果。

综合 seed=42~49 的 56 个点，最终方案为 **48 胜、8 平、0 负**，平均请求计算
占比从 72.142% 提高到 75.252%（+3.111pp）。分 SSU 的八 seed 均值如下：

| SSU | 当前 | 最终方案 | 提升 |
|---:|---:|---:|---:|
| 8 | 34.448% | 44.491% | +10.043pp |
| 16 | 51.563% | 60.100% | +8.538pp |
| 28 | 71.828% | 73.274% | +1.445pp |
| 40 | 80.871% | 80.871% | 0（保守回退） |
| 56 | 86.589% | 87.161% | +0.572pp |
| 80 | 89.260% | 90.068% | +0.809pp |
| 112 | 90.433% | 90.801% | +0.368pp |

## 5. 离“最优”还有多远

对同一物理容量模型又搜索了 clairvoyant、每 SSD 可见工作优先级的 demand 指数。
最佳单一指数为 `alpha=0.025`。在最终 seed=48/49 上：

| 策略 | 请求计算占比 | Fleet | Jain | Makespan |
|---|---:|---:|---:|---:|
| 当前 | 72.633% | 15.446% | 0.8576 | 2047.43 ms |
| 最终可部署方案 | 75.616% | 15.451% | 0.8667 | 2047.24 ms |
| Clairvoyant 可行参考 | 77.911% | 14.856% | 0.9059 | 2144.18 ms |

最终方案填补了当前到该参考差距的约 **56.5%**。参考策略不是数学上证明的全局
最优，而且它为提高请求平均值明显牺牲 Fleet 和 Makespan，所以剩下的 2.295pp
不能理解为没有代价的可拿收益。

## 6. 必须保留的风险和硬件边界

1. **这仍是 Path 级 CIR 仿真，不是真正的类别父队列。** 均匀的 4 Path/类消除了
   “某类因为 Path 更多而天然得到更多票”的主要偏差，但当前后端仍按 Path token
   和 Group/Path WRR 服务。若实现真正的 8 个父队列 CIR，必须重新跑本搜索。
2. **前置 8 队列 WRR 和 6849 深度没有被显式建模。** 8 个 Q 类可以自然映射到
   8 个硬件队列，但只有硬件确实按这些类别做 WRR/CIR 时才等价。队列深度主要
   影响溢出、反压和排队尾延迟，不直接决定 40 GB/s 的服务份额。
3. **并非所有旧类别都同时改善。** 最终盲测中 SL 提升最大；LS 平均计算占比基本
   持平。SS/LS 的平均 p95 TTFT 分别增加约 47.7/18.3 ms，最大 TTFT 也略升。
   如果目标是严格限制尾延迟，需要把 p95/最大 TTFT 加入多目标搜索，而不能只
   最大化平均请求计算占比。
4. **没有验证表外 SSU 数。** 例如 32、48 SSU 应重新运行搜索或至少做独立验证，
   不应直接套用 40 SSU 的特殊回退结论。

## 7. 代码和结果

- `sim.py`：支持任意数量静态 QoS 路由类，并同时记录原类别和新路由类别。
- `strategy_profiles.py`：固化两个 8 类 profile 和保守选择器。
- `optimize_qos_profiles.py`：可恢复 checkpoint 的静态 profile 搜索。
- `optimize_oracle_priority.py`：可行 clairvoyant 参考的指数搜索。
- `tests/test_qos_profile_optimization.py`：8 类 Path 分区、分类、候选和最终选择器测试。
- 最终盲测原始结果：
  `results/qos_profile_optimization/stage9_conservative_blind_seed48_49.json`。
- 最终 oracle 原始结果：
  `results/qos_profile_optimization/oracle_reference_blind_seed48_49.json`。

复现最终盲测：

```bash
python optimize_qos_profiles.py \
  --seeds 48,49 \
  --ssus 8,16,28,40,56,80,112 \
  --workers 10 \
  --output results/qos_profile_optimization/stage9_conservative_blind_seed48_49.json \
  --profiles low_protect_cir_20_6_8_6_current_paths,score8_a0p0_p3p0_floor0p25,score8_a0p2_p2p0_floor0p5
```
