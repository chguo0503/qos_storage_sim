# Preserve exported evidence byte for byte. Large generated CSV/SVG diffs are
# not useful line-oriented reviews; the generators and audit hashes are tracked.
*.csv -text -diff
*.svg -text -diff
*.gz binary
*.png binary
*.pdf binary
*.pptx binary
*.zip binary
