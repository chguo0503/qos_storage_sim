同一冻结输入、seed 7 下，OD 在 20–40 秒和 40–60 秒的平均 NPU 利用率分别为 79.3217%、80.1752%；Once（流量分配）分别为 98.9450%、98.9028%。这两个预先固定的中后段窗口内，32 张卡持续活跃，并且每张卡都运行了长短请求，没有使用排空尾部。

这是一次针对 OD 提前选定输入的正式重放。请求在运行前已经全部冻结，正式指标没有使用离线规划的额外事件钩子；标准 API 的策略适配器照常工作。

比较的是当前两套完整策略配置。OD 每盘使用 32 条 NPU 独占 Path，每卡 CIR=40/32=1.25 GiB/s，空闲带宽可借用。Once 配置为 256 条可用 Path，保留原来的类别候选池和静态 CIR：SS/SL/LS/LL 的每盘类别总预算分别为 20/6/8/6 GiB/s，候选 Path 数分别为 96/32/96/32；本输入的 A 属于 LS、B 属于 LL。Once 每层、每盘使用一次拥塞快照来规划这一层各 I/O 的 Path，不是整层只选一条 Path。因此结果包含路径数量、类别 CIR/候选池与选路的共同作用，不能说成“只改变选路”的单变量实验。

它检验的是“是否存在让 OD 长时间平均利用率偏低的输入”，不是普通 data 随机输入发生问题的概率，也不是每一时刻严格欠载的实验。每盘的瞬时需求是否超限，以下面逐事件重算为准。

| 统计区间 | 策略 | NPU 平均利用率 | SLO ×1.5 达标率 | 窗口接纳请求数 | 持续活跃卡 | 都计算过长短请求的卡 |
|---|---|---|---|---|---|---|
| 20–40 秒 | OD | 79.3217% | 67.5676% | 1184 | 32 / 32 | 32 / 32 |
| 20–40 秒 | Once（流量分配） | 98.9450% | 99.8612% | 1441 | 32 / 32 | 32 / 32 |
| 40–60 秒 | OD | 80.1752% | 66.6667% | 1152 | 32 / 32 | 32 / 32 |
| 40–60 秒 | Once（流量分配） | 98.9028% | 99.3060% | 1441 | 32 / 32 | 32 / 32 |
| 20–60 秒 | OD | 79.7484% | 67.1233% | 2336 | 32 / 32 | 32 / 32 |
| 20–60 秒 | Once（流量分配） | 98.9239% | 99.5836% | 2882 | 32 / 32 | 32 / 32 |

补充核对了全部 23 个完全落在 20–60 秒内的完整 ABB 周期：OD 的逐卡完整周期加权计算占比为 79.6745%–80.3228%，周期末卡间完成时刻最大跨度为 0.182689 ms。这个周期指标使用每卡自己的周期区间，不等于同一个墙钟窗口的整机利用率，不能替换上表；它用于检查现象是否反复出现。

图上的 2 秒窗口与 1.65 秒请求周期并不对齐，有些窗口多包含一次等待段，所以细线高低会变化；不能把这种采样后的波形直接当成请求本身的周期。长期判断用预先固定的宽窗口均值，同时保留所有 2 秒窗口，没有挑选低谷。

利用率按窗口内真实计算时间 / (32 × 窗口时长) 计算。SLO 按窗口内上卡的请求计算，并等待这些请求真正完成后统计；耗时从上卡开始到 prefill 完成，不含卡内排队，阈值是该请求自身 8 层纯计算时间的 1.5 倍。这不是完整端到端 TTFT。没有丢弃窗口末尾未完成的请求。

| 20–60 秒策略 | 请求类型 | 该类型驻留期间的计算占比 | 占全部窗口卡时间 | 对整机 U 的贡献（百分点） |
|---|---|---|---|---|
| OD | 短计算请求 | 14.2327% | 20.6702% | 2.9419 |
| OD | 长计算请求 | 96.8192% | 79.3298% | 76.8065 |
| Once（流量分配） | 短计算请求 | 93.5525% | 3.8657% | 3.6164 |
| Once（流量分配） | 长计算请求 | 99.1399% | 96.1343% | 95.3075 |

整机 U 是最后一列相加，不是把两个类别各取一半，也不是按请求数 1:2 加权。OD 的长请求内部各层读得及，不等于整个长请求零等待：它的首层仍可能等前一短请求最后一层没能完全藏住的读取。

窗口中的请求身份会因策略推进速度而变化；因此还应看同一批全部请求：

例如 20–60 秒的上卡队列：OD 包含 2336 个请求，Once 包含 2882 个，身份交集为 2098 个；这不是完全相同的窗口样本。下表使用全部 4800 个相同请求，避免把窗口推进差异混入请求集合。

| 策略 | 相同完整请求人口 | 达标数 | SLO ×1.5 达标率 | 归一化耗时 p95 |
|---|---|---|---|---|
| OD | 4800 | 3200 | 66.6667% | 7.3647 |
| Once（流量分配） | 4800 | 4662 | 97.1250% | 1.2654 |

完整人口 SLO 包含启动和排空期的请求，只用于同一请求集合的补充核对。长期利用率结论使用前表的固定中段窗口，不使用带排空尾部的全程平均。p95 为排序后第 ceil(0.95 × n) 个请求。

每卡输入为“短计算请求 A → 长计算请求 B1 → 长计算请求 B2”，重复 50 次。全部 arrival=0；32 NPU、3 SSU、每盘 40 GiB/s、8 层、batch=1、seed 7、原始 RingHash。每个请求的物理身份唯一，没有为落盘比例搜索地址。

| 请求位置 | 整机数量 | 总输入长度 K | miss tokens | 每层计算 ms | 每层读取 MiB |
|---|---|---|---|---|---|
| A | 1600 | 128 | 256 | 6.024012 | 175.65625 |
| B1 | 1600 | 128 | 3328 | 75.603838 | 171.53125 |
| B2 | 1600 | 127.931–128.49 | 3257–3830 | 73.956998–87.293717 | 171.53125 |

A 直接使用 data 的 128K / miss 256。B 的计算时间来自 data 中长度 96/128/160K 与 miss 2048/4096 网格的双线性内插，无外推；固定命中 127744 tokens，miss 是整数，总长度随 miss 改变，读取量保持一致。它不是每个点都已有原始实测的 workload。

通俗地说，每轮预先定为 1.65 秒：第一轮目标在 1650 ms 结束，第二轮在 3300 ms 结束，以此类推。先在离线规划中看每张卡开始计算 B2 的时间 s，再在允许的整数 miss 中选最接近 C=(E-s)/8 的请求，E 就是预先固定的周期末时刻，E、s、C 都用毫秒。这样尾部长请求完成时间会重新靠拢，下一轮短计算请求又容易一起到来。正式实验重新读取冻结输入，策略无法临时延长计算或等待。

还可以用冻结后的输入反算一次：每卡平均共需纯算 65.890887 秒；若全部卡按设计在 82.5 秒完成，则利用率应为两者之比，即 79.867741%。这是看过规划后、对冻结输入的条件式算术核对，不是独立的事前预测；上表仍以正式重放的逐层积分为准。

每个 B2 完成时刻到目标时刻的量化误差上界为 0.093808 ms，两个卡之间的理论量化差上界为 0.187615 ms。并非所有 B2 都增加计算：有 7 个缩短；相对基准 B，净计算变化 95.942780 卡秒、绝对变化 96.015161 卡秒。所有这些计算都进入利用率分子和自身 SLO 基线。

| 策略 | 时间段（秒） | SSU | 时间平均需求 GiB/s | 最高需求 GiB/s | 需求 >40 的时间占比 |
|---|---|---|---|---|---|
| OD | 20–40 | 0 | 77.3595 | 291.0006 | 22.3884% |
| OD | 20–40 | 1 | 85.1128 | 319.6715 | 22.5363% |
| OD | 20–40 | 2 | 82.6849 | 311.1455 | 22.5362% |
| OD | 40–60 | 0 | 75.3610 | 291.8086 | 21.4968% |
| OD | 40–60 | 1 | 82.7551 | 319.1700 | 21.7079% |
| OD | 40–60 | 2 | 80.7221 | 310.2817 | 21.7078% |
| OD | 20–60 | 0 | 76.3603 | 291.8086 | 21.9426% |
| OD | 20–60 | 1 | 83.9339 | 319.6715 | 22.1221% |
| OD | 20–60 | 2 | 81.7035 | 311.1455 | 22.1220% |
| Once（流量分配） | 20–40 | 0 | 31.5648 | 65.1218 | 14.4516% |
| Once（流量分配） | 20–40 | 1 | 34.7143 | 69.6685 | 36.4063% |
| Once（流量分配） | 20–40 | 2 | 33.7741 | 68.6976 | 33.2341% |
| Once（流量分配） | 40–60 | 0 | 31.6071 | 70.4169 | 12.8141% |
| Once（流量分配） | 40–60 | 1 | 34.6937 | 76.4550 | 35.3422% |
| Once（流量分配） | 40–60 | 2 | 33.9067 | 78.2629 | 32.0388% |
| Once（流量分配） | 20–60 | 0 | 31.5859 | 70.4169 | 13.6329% |
| Once（流量分配） | 20–60 | 1 | 34.7040 | 76.4550 | 35.8742% |
| Once（流量分配） | 20–60 | 2 | 33.8404 | 78.2629 | 32.6364% |

这里的需求 D_s(t) 是当前每张卡正在处理的请求的 V_i,s / C_i 之和，I/O 等待中的卡也计入；下一请求首层预取不再额外叠加一个请求画像。OD 在这里是间歇性强过载，Once 也仍有超限时段；都不能称为任意时刻逐盘欠载。超限占比只数持续时间，不数超限有多高或哪些请求赶不上读取期限，因此不能单独用它预测利用率；Once 某盘的超限占比虽更高，但峰值和时间平均需求已经明显降低。

另外一种容易混淆的数字是“按每卡总读取量 / 每卡总纯计算时间，再把各卡相加”的工作量参考速率：SSU 0: 31.0217, SSU 1: 34.1239, SSU 2: 33.2594 GiB/s。它没有描述请求在墙钟时间上的同时出现情况，即使低于 40，也不能证明执行中欠载。

只按这批工作的总量看，计算最多的卡至少要 66.0619 秒；数据最多的盘以 40 GiB/s 读完至少要 56.2111 秒。因此全程完成时间至少为两者较大值 66.0619 秒，全程利用率的相应宽松上界为 99.7412%。这是工作总量给出的必要界，不是最优策略可达到的保证，更不是 20–40 秒窗口的上界。

| 策略 | SSU | 供给统计时间（秒） | 全程真实物理供给均值 GiB/s |
|---|---|---|---|
| OD | 0 | 0–82.500093 | 24.776290 |
| OD | 1 | 0–82.500093 | 27.253827 |
| OD | 2 | 0–82.500093 | 26.563544 |
| Once（流量分配） | 0 | 0–67.940683 | 30.085748 |
| Once（流量分配） | 1 | 0–67.940683 | 33.094210 |
| Once（流量分配） | 2 | 0–67.940683 | 32.256003 |

上述供给是每盘真实完成字节 / 全程时长，包含启动与排空；不能直接减去 20–60 秒的需求均值解释成同一窗口的缺口。raw 没有任意窗口的物理服务事件，因此不虚构这类供给曲线。

[layer_cycle_supply.csv](layer_cycle_supply.csv) 则记录完整层周期的真实读取量与平均供给：从本层计算开始到下一层计算开始，下一层全部读取在这个周期内完成，所以平均 b=V_next/周期时长。已逐周期检查 I/O 提交和就绪端点。跨请求使用下一请求首层的 V；异步周期均值不是瞬时速率，裁窗口边界也不能更换平均的分母。

一个真实例子：预先选定图示区间内，NPU 0 的首个完整短请求内部周期（请求 72，第 2 层到下一层）读取 175.65625 MiB；计算 C=6.024012 ms，随后等待 I=42.189947 ms，完整周期 T=48.213959 ms。所以需求 B=V/C=28.4759 GiB/s，完整周期供给 b=V/T=3.5579 GiB/s，b/B=C/T=12.4943%。这是同一个请求内部的完整周期等式，不能机械套到跨请求边界或任意瞬时。

需要保留的限制：这个输入是专门针对 OD、seed 7 和当前配置挑选的，对 Once 没有重新选择 miss；不能推断其他种子或真实随机 workload。离线同步依赖确定性的计算/服务时序；真实系统的抖动可能破坏它，本轮没有验证这类鲁棒性。两策略虽运行同一冻结请求集合，但完成速度改变当前活跃请求身份，因此每时刻的需求曲线不必相同。OD 仍为每卡每盘独立 Path、每盘固定队深 8192（每卡 256）；Once 使用此前原始策略默认的无限队深。CIR 是可借用空闲带宽的保底，不是每卡永远只有 1.25 GiB/s 的上限；队深不能借用也不等于带宽不能借用。

队深差异也做了同输入消融：把 OD 的每卡每盘 256 深度改成无限，全部 4800 个请求的时间字段、38400 层的完整时序记录都与有限深度相同。因此这次 OD 利用率损失不是该队深上限造成的。只有从实际入盘开始计的等待诊断变化，反映等待位置在主机与盘侧之间移动；不能把诊断缩短当作请求加速。这项结论只针对本输入。

另把 OD 的同刻提交顺序 seed 从 7 改为 19，输入字节不变，全部请求和层记录仍严格相同。这个种子变化在本例中没有有效扰动 OD 时序，因此不能包装成另一批随机工作负载，也不能当作抗计算/SSD 服务抖动的证据。

独立验证已覆盖：manifest 的全部块落盘、数据内插与整数 miss、来源文件 SHA 链、规划/正式 OD 的所有请求和层时序严格相同、每层计算时间、每卡计算+I/O 等待守恒、所有连续 2 秒窗口的活跃情况、完整人口 SLO、逐事件需求以及层周期供给端点。正式数据都已排空，OD 和 Once 的输入 manifest SHA 完全相同。

原始数值见 [audit.json](audit.json)、[windows.csv](windows.csv)、[cycles.csv](cycles.csv)、[population_slo.csv](population_slo.csv)。

- [scheduled_long_term_utilization](../../figures/scheduled_long_term_utilization.png)
- [scheduled_od_two_cycles](../../figures/scheduled_od_two_cycles.png)
- [scheduled_od_demand](../../figures/scheduled_od_demand.png)
- [scheduled_once_two_cycles](../../figures/scheduled_once_two_cycles.png)
