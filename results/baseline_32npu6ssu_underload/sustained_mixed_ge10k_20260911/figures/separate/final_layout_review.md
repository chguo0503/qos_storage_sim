14张独立图已全部完成并逐张打开PNG目视检查；每图同时保存PNG/PDF/SVG，共42个图形文件。没有新增仿真或重放。

8张32卡时间线逐卡核对真实计算＋暴露等待等于窗口时长、三种计算之和等于全部计算、全部32卡活跃且实际含快短与Long。图中U与原始窗口独立核对：主窗直接对照[2,60)，细节窗对照5个相邻2秒窗口。116个独立2秒数据点均来自完整结果，无缺失填充。

| 输入 | 策略 | U[2,60)% | U[2,12)% | 全程名义最热盘GiB/s |
|---|---|---:|---:|---:|
| random | baseline | 99.997867288 | 99.992793381 | 36.428823953 |
| random | once | 99.987211092 | 99.976791808 | 36.586561460 |
| ordered | baseline | 89.964747176 | 89.794543177 | 36.011187978 |
| ordered | once | 99.961111415 | 99.967427527 | 36.021873262 |

图例统一为蓝色“短请求”、紫色“桥接请求”、绿色“长请求”，橙色仍为真实接纳后等待。蓝色仅含NQL1024请求；紫色32K/NQL4096桥接独立计费，分类和数值不变。图注说明有限批次、全部计算计费、无运行时屏障；未把桥接计算称作快短。

名义需求图保留每盘40GiB/s容量线，明确当前请求D/C和跨请求L0的口径；不是盘busy或瞬时释放工作。后半段交接图使用[51100,51520)ms共同绝对轴，读取生命周期与计算预算分开，不指认某具体Long为FIFO队头。

所有图的标题、图例、坐标和脚注均在画布内，目视未见裁字或文字重叠。完整58秒时间线的细粒度等待可能小于像素；另有[2,12)细节图和真实后半段交接图。详见[源哈希、图哈希及逐项审计](final_layout_review.json)。

本次图例改名复核：9张带类别图例的PNG均重新打开目视检查，三个新标签正确、无文字裁切或重叠；9份PDF文字提取均含三个新标签、不含旧图例标签，全部PDF文字边界在页面内。14份图形审计的原有数值和5份原始结果gzip哈希与[改名前快照](label_rename_before.json)逐项完全一致。另5张不含类别图例的PNG与此前目视检查版本字节相同。已刷新全部42个PNG/PDF/SVG哈希和渲染脚本哈希，未运行仿真。
