# 带宽交付图最终核验

已生成36张独立图，每张提供PNG、PDF、SVG，共108个图文件。所有文件SHA与`render_audit.json`逐项匹配；源分析和绘图器SHA也匹配。36张图的自动文字画布边界检查及PDF文字提取检查全部通过。

目视检查最终版`npu_16`、`npu_31`、`overview_baseline`、`cumulative_examples`与`stall_despite_high_bandwidth`：无裁切、文字重叠或曲线裁峰。累计图与反例图已加大底部留白，坐标标签和首行脚注分离。目视是抽样，自动画布边界检查并不证明任意对象之间都不重叠。

红色D/C只作为名义参考；绿蓝曲线由真实SSD/链路服务片段积分得到。单卡和概览为[3.2,4.0)秒；累计图按同一真实层的各策略release分别对齐且同时显示绝对时间。均匀完成参考曲线封顶于D，中途落后不被误标为stall；只在真实截止时检查缺口。高带宽反例表明预算期内没交付、等待期内均值超过D/C仍可同时成立，未声称每一瞬间带宽均高于D/C。

详细检查与PDF/源文件哈希见`visual_review.json`；全部108文件哈希见`render_audit.json`。此步骤仅离线绘图与读取既有结果，没有运行仿真。
