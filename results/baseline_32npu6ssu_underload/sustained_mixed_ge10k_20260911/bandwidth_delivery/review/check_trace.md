# 两份物理 trace 独立复核

离线检查已完成，Baseline 与 Once 均通过；没有导入模拟器、启动或重跑仿真。

| 策略 | 完整捕获块数 | 显示窗层数 | 额外同 ID 层数 | 物理区间重叠 | 分箱字节误差 | 与主分析最大带宽差 |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 813656 | 1893 | 0 | 0 | 0 GiB | 2.84e-14 GiB/s |
| once | 896904 | 2141 | 2 | 0 | 0 GiB | 2.84e-14 GiB/s |

逐盘 SSD 和逐 NPU 链路分别检查不重叠；逐块时长分别精确等于 size/40 与 size/50。0.5 ms 分箱使用独立通用区间交集循环，逐箱对照主分析，未复用其积分函数。显示窗为 [3200,4000) ms。

完整目标层重新从冻结参考结果选取，并补三个预先明确的同 ID 示例键；逐块索引、NPU、SSU、大小与 manifest 完全一致，最后 HBM 就绪时刻等于原层记录。没有用 trace 内的 passed 标志替代这些独立检查。前缀全部运行状态相等的检查另外继承已绑定 SHA 的 replay audit。

| 示例 | Baseline stall ms | Once 同 ID stall ms | Baseline 截止 SSD/HBM 缺口 MiB |
|---|---:|---:|---:|
| (16, 0) | 0.000000000000 | 0.000000000000 | 0.000000/0.000000 |
| (22000046, 4) | 9.001549253529 | 0.000000000000 | 42.625000/42.625000 |
| (26000038, 5) | 9.793626125725 | 0.000000000000 | 42.625000/42.625000 |

另例 NPU24/rid24000046/L2：截止时 SSD 与 HBM 均未收到该层任何字节，均缺42.625 MiB；之后 stall 6.821147258641 ms 内，目标层实际 SSD 服务均值 6.102489065863 GiB/s，高于预算 5.735792789429 GiB/s。说明等到逾期后再获得较高带宽，仍不能消除已经发生的等待。这个键未额外采集 Once 物理 trace，不能补造同 ID 的 Once 物理服务曲线。

Long/rid16/L0 的目标量240.625 MiB，前驱 bridge C=28.592841996 ms，真实预取预算8.218327916 GiB/s。Long 自身 D/C=7.479698789；而实际预取期间当前请求仍是 bridge，其自身名义参考只有1.314932467。真实下一层预算为当时名义参考的6.25倍，不能混用。

两个 Once 同 ID 短例分别发生于约2.963秒、2.506秒，早于显示窗；作为明确额外捕获的完整例子保留。它们不是与 Baseline 在同一绝对时刻执行，也没有被计入 [3.2,4.0) 带宽箱。

窗口中 SSD 与 HBM 总字节不必相同：前者是读服务，后者是链路服务，窗口边界可能留下已读待传的数据。应分别守恒，不能要求二者在任意有限窗口内完全相等。

首版检查器只允许显示窗层集合，因 Once 额外两层导致集合检查未过；其物理检查全部通过。首版输出保留在 initial_display_only_check/，最终版显式加入已声明的三个示例键，并重新独立核对来源和完整块。这个审计脚本口径修正不是重放失败。

可复算：`python bandwidth_delivery/review/check_trace.py --require-complete`；详细原始结果为 `baseline_trace_check.json`、`once_trace_check.json` 与 `trace_checks.json`。
