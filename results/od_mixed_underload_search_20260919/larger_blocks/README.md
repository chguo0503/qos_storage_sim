# A180/B1039 原仿真短试验

10 个候选已完成。有效候选要求 [0,4) 全程逐盘严格欠载、[2,4) 32 卡持续活跃且每卡实际计算过两类请求。8 个通过，2 个虽 warm 欠载但早期超限，已淘汰。最优有效候选的 warm U 为 **96.114984%**，没有优于此前 A80 候选的 95.90345%。

这里只回放至 4 秒，没有完整排空；不提供 TTFT SLO，也不声称长期维持这个 U。

最优 `larger180_grid_06`：A 每层 180 个 176 KiB 块、计算 4.029030293797608 ms；B 每层 1039 块、计算 79.48894933249836 ms。4 组各 8 卡，初始 B 前缀分别为 2、4、1、3，此后每卡重复 `A×14 → B×3` 六次。A 总输入为 22.75 Ki token（256 新增），B 为 133.875 Ki token（4096 新增）。计算时间为合成值，不是 data 原始测量。

物理 request ID 从 200000000 起按公开固定序列搜索，选取 A 在 SSU1 恰好 62 块、B 恰好 360 块且另两盘分别不更多的请求。所有 ID 不同，使用原 Ring Hash 函数。它属于对抗性地址选择，不能称普通随机输入。

最优候选 [0,4) 最忙盘峰值为 38.907011 GiB/s。warm 中每卡至少计算 A 127.028 ms、B 1425.167 ms。该候选恰好六个完整循环，无额外 A 尾巴，因此两个冻结 manifest 人口完全相同。

- `pilot_summary.csv`、`summary.json`：10 组完整结果及是否通过约束。
- `inputs/frozen_exact_larger180_grid_06.json.gz`：与最优 pilot 完全相同的输入，供正式完整回放。
- `inputs/frozen_6cycles_larger180_grid_06.json.gz`：相同人口的六循环版本。
- `address_pool.json`：地址筛选规则、命中地址、逐盘块数和核心源码哈希。
- `prelaunch_revision.json`：最后两组在启动前依据已完成结果调整参数的记录。
- `pilots/*/command.json`：使用原 `run_pilot.py` 的精确测量、源代码未变检查；没有修改核心仿真。

10 组在本地 CPU 8、10、12 上并行运行，共约 313 秒。地址池生成约 19 秒。
