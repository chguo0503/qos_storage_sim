# CIR 更新周期该怎么设

> 针对的问题：每块 SSU 有 256 个 QoS Path，它们的 CIR 寄存器应该多久刷新一次？
>
> 相关：`ADAPTIVE_POLICY_IO.md`（策略输入输出）、`sim.py:750`（CIR 提交入口）

---

## 0. 结论先行

**当前实现里「策略计算」和「CIR 写入」是硬绑定的：算一次就写一次。**

```
   实测 evals : cir_commits
      SSU24    249 : 248     = 1.00
      SSU40    166 : 166     = 1.00
      SSU70    131 : 131     = 1.00
```

你说「策略可以在客户端不停地算」——这完全没问题，策略本身很便宜（128×70 只要
1.4 ms）。**贵的是写寄存器。** 所以正确的做法是把这两件事解耦：

```
   现在：  算 ──────────► 写 256×N_ssu 个寄存器
   应该：  算 ──► 过滤 ──► 只写真正变了的那些
```

**推荐配置：保持 25 ms 决策间隔不变，加一个 0.05 GB/s 的写入阈值。**
这样开销大降而 SLO 不受影响——因为策略仍按原节奏跟踪负载。

---

## 1. 先搞清楚三个不同的「周期」

混淆通常出在这里：

| 名称 | 当前值 | 是什么 |
|---|---:|---|
| `min_interval_ms` | 25 ms | **你能配的参数**。是下界，不是周期 |
| 实测平均间隔 | 8~15 ms | 两次决策的实际平均距离 |
| CIR 写入周期 | **= 决策周期** | 当前没有独立控制 |

### 为什么平均间隔（8 ms）会小于最小间隔（25 ms）

因为 25 ms 只管 `batch_boundary` 这一类触发（`continuous_batch_sim.py:1136`）：

```python
if (reason == "batch_boundary"
        and context.last_control_ms is not None
        and context.control.min_interval_ms > 0.0):
    scheduled_time_ms = max(scheduled_time_ms,
                            context.last_control_ms + min_interval_ms)
```

而且它是**推迟不是丢弃**：不足 25 ms 的触发被推到 `last_control + 25ms`，同一
时刻的多个触发合并成一次。所以实际节奏由事件密度决定，25 ms 只是软下界。

**当前系统没有固定的 CIR 更新周期。** 它是事件驱动的。

---

## 2. 开销到底有多大

每块 SSU 有 256 个 Path，128 个 NPU 各占一个专属 Path，所以每块盘**最多 128 个
Path 需要更新**。实测：

```
 SSU   commits      总写入    每次提交写      每盘每次写
  24       248      754209        3041       127 / 128     <- 99% 全重写
  40       166      844631        5088       127 / 128     <- 99% 全重写
  70       131      611236        4666        67 / 128     <- 52%
```

**SSU24/40 每次提交几乎把该盘所有 128 个活跃 Path 全部重写一遍。**

换算成寄存器写入速率：

```
 SSU   每盘总写入(2s)   每盘每秒(个)   提交次数/秒   平均决策间隔
  24       31425         15713          124.0        8.0 ms
  40       21116         10558           83.0       12.0 ms
  70        8732          4366           65.5       15.3 ms
```

**每块盘每秒上万个寄存器被改写**——这才是真正的成本，不是策略计算。

注意「每盘每秒 15713 个」不等于「每秒 15713 次提交」：SSU24 每秒只提交 124.0 次，
但每次要写 127 个 Path 寄存器，`124.0 × 127 ≈ 15713`。

---

## 3. 方案 A：放长 `min_interval_ms`（拿 SLO 换开销）

项目里有现成的 25ms vs 50ms 对照实验（唯一变量就是这个参数）：

```
                        提交次数     寄存器写入
 SSU  设置    commits    每秒       每盘每秒       util       SLO
  40  25ms       166     83.0        10558       51.57%    82.45%
  40  50ms        85     42.5         5376       51.70%    75.23%
      变化                -49%         -49%      +0.13pp   -7.22pp

  70  25ms       131     65.5         4366       90.85%    98.88%
  70  50ms        65     32.5         2587       90.78%    92.26%
      变化                -50%         -41%      -0.06pp   -6.61pp
```

> **注意两列的单位不同。** 「提交次数/秒」是控制器推了多少次 CIR 表；
> 「寄存器写入/盘/秒」是实际被改写的 Path 寄存器**个数**。
> 一次提交要把该盘 128 个活跃 Path 各写一遍，所以后者 ≈ 前者 × 每次写的个数。
>
> 以 SSU70 / 50ms 为例：
> ```
>    cir_path_writes = 362115 个,  cir_commits = 65 次,  70 块盘,  2 秒窗
>
>    每次提交写   362115 / 65      = 5571 个（全部 70 块盘合计）
>    分摊到每盘   5571 / 70        =   80 个/次
>    每盘每秒     65/2 × 80        = 2587 个/秒
> ```
> **开销正比于寄存器个数，不是提交次数**——推 127 个寄存器和推 3 个，成本差 40 倍。
> 这正是方案 B 更划算的原因（见下）。

**写入减少 41~49%，利用率纹丝不动（±0.13 pp），但 SLO 掉 6.6~7.2 pp。**

这符合前面报告的结论：利用率被吞吐钉死，策略的价值全在 SLO 上，所以放松控制
**先伤 SLO**。

### 结论：这个方向有代价，不是免费的

如果你的 SLO 预算允许（比如 92% 够用），SSU70 用 50 ms 是纯赚。但重过载点
（SSU24）恰恰决策最密（8 ms）也最需要密，**不该放松**。

---

## 4. 方案 B：写入阈值过滤（推荐，几乎免费）

### 机制已经存在，只是形同虚设

`sim.py:772-778`：

```python
changes = [
    (path_id, cir)
    for path_id, cir in enumerate(path_cirs)
    if abs(self.paths[path_id].cir - cir) > _EPS     # _EPS = 1e-12
]
if not changes:
    return 0
```

**`_EPS = 1e-12` 等于没有过滤。** 任何浮点差异都会触发写入。

对比 `current_scheme_b`：它评估了 47492 次却只提交 2902 次（16:1），因为它算出
的 CIR 大多真的没变。而 V2.1 的 grants 每次都有微小变化，全部通过。

### 改法：一处改动

```python
changes = [
    (path_id, cir)
    for path_id, cir in enumerate(path_cirs)
    if abs(self.paths[path_id].cir - cir) > self.cir_write_threshold
]
```

加一个可配置字段，默认 0.05 GB/s。

**没有漂移风险**：比较基准 `self.paths[path_id].cir` 是**实际写入的值**，不是
理论值。所以小变化被过滤后，下次比较仍以旧值为基准，偏差不会累积——只要累计
变化超过阈值就会被写入。

### 为什么 0.05 GB/s 是安全的

相对 40 GB/s 的盘带宽，0.05 是 **0.125%** 的分辨率。而 CIR 只是仲裁权重，不是
精确速率（报告 §8.1：「CIR 不是精确瞬时速率」）。这个量级的差异对仲裁结果没有
可观测影响。

---

## 5. 两个方案对比

```
                        提交次数   每次写的个数   总寄存器写入    SLO       性质
   现状 (25ms)            基准        ~127          基准       98.88%
   方案A: 放长到 50ms     减半        ~127        -41~49%     -6.6pp    拿 SLO 换开销
   方案B: 阈值 0.05       不变        待测          待测        不变     几乎免费
   A + B 叠加             减半        待测         更低        -6.6pp    可叠加
```

两个方案打在**不同的乘数**上：

```
   总开销  =  提交次数  ×  每次写的寄存器个数
              ▲              ▲
              |              |
           方案A 压这个    方案B 压这个
```

方案 B 直接打在真正的成本项上，且不改变策略的跟踪节奏，所以优先做。

**方案 B 优先做**，因为它不改变策略的跟踪节奏，只是不做无意义的寄存器更新。

---

## 6. 推荐配置

### 如果只改一处

**加 0.05 GB/s 写入阈值，`min_interval_ms` 保持 25 ms 不变。**

### 如果要按负载分档

从数据看，过载点和轻载点的需求正好相反：

```
   SSU70  fraction=1.0000  全走 V1  决策最稀(15.3ms)  SLO 余量大  -> 可以放到 50~100ms
   SSU40  fraction=0.9219  全走 V1  中等(12.0ms)                 -> 50ms
   SSU24  fraction=0.5859  走 V2    最密(8.0ms)      SLO 最紧    -> 保持 25ms
```

可以按 `selected_fraction` 动态调整：

```
   selected_fraction >= 0.95   ->  min_interval = 50 ms      (轻载)
   0.75 <= fraction < 0.95     ->  min_interval = 25 ms      (中等)
   fraction < 0.75             ->  min_interval = 25 ms      (过载,不放松)
```

**注意**：这是基于当前三个工作点的推断，跨过阈值时的抖动风险未验证，上线前
需要单独实验。

### 生产环境的额外考虑

报告 §7.4 指出真实系统的主要成本是 **N_ssu × 256 Path 表的寄存器扇出**。
除了上面两条，还可以：

1. **批量写**：如果硬件支持一次写多个 Path 寄存器，把 127 次单写合并成若干批。
2. **跳过零值 Path**：每块盘 256 个 Path 里只有 128 个被 NPU 占用，其余恒为 0，
   不需要每次都写。当前实现已经通过 delta 过滤天然跳过了它们。
3. **只在跨 epoch 时全量提交**：报告 §7 的生产架构建议用 generation + 两阶段
   提交，正常情况下只推 delta。

---

## 7. 实测：阈值能省多少

已在真实 DES 上做过只读观测（包一层 controller 记录每次决策的 grants，不改变
任何行为；结果 `util=0.3251 SLO=0.5210 writes=754209` 与冻结基线完全一致）：

```
  128 NPU x 24 SSU,  249 次决策,  每次 3072 个格子

   阈值(GB/s)     需重写比例      每次写入数     相对基线
      0.000          99.3%          3051         基准
      0.001          77.8%          2390         -22%
      0.010          33.5%          1028         -66%
      0.050           8.7%           268         -91%
      0.100           8.6%           263         -91%
      0.250           7.6%           233         -92%
      0.500           2.9%            90         -97%
      1.000           2.5%            78         -97%
```

三个结论：

1. **0.05 GB/s 一档就砍掉 91% 的写入**（每次 3051 → 268 个寄存器）。
2. **0.05 到 0.25 是平台期**（8.7% → 7.6%）。存在一个"必须重写"的底座——很可能
   是请求进入/离开活跃集导致的整行变化，这部分无法用阈值消除。所以 **8% 左右
   是这个方案的下限**，阈值再大收益也有限（除非到 0.5，但那已接近 40 GB/s 的
   1.25%，可能影响仲裁）。
3. 基线的 **99.3%** 印证了前面的判断：当前几乎每次全量重写。

### 但这还不能下结论

**这份数据只说明"可以少写多少"，不说明"少写之后 SLO 会怎样"。** 必须真跑带
阈值的 DES 才能知道。任务规格见 `TASK_CIR_WRITE_THRESHOLD.md`。

如果 SLO 在 0.05 档下降超过 2 pp，说明那些"微小"的 CIR 变化对仲裁确实有实质
影响，本方案不成立——这也是有价值的结论。

### SSU70 的结果推翻了上面的外推

同样的只读观测（`util=0.9085 SLO=0.9888 writes=611236`，与冻结基线一致）：

```
  128 NPU x 70 SSU,  131 次决策,  每次 8960 个格子

   阈值(GB/s)     需重写比例     每次写入数    相对基线      对比 SSU24
      0.000          74.0%         6627        基准        (99.3%)
      0.010          47.5%         4260        -36%        (-66%)
      0.050          31.5%         2826        -57%        (-91%)
      0.250          12.7%         1142        -83%        (-92%)
      0.500           5.4%          487        -93%        (-97%)
```

**两个工作点行为完全不同**：

- SSU24 在 0.05 就省 91%，且 0.05~0.25 是平台期；
- SSU70 在 0.05 只省 57%，且是一条**平滑曲线，没有平台**。

我此前预测「SSU70 可过滤比例更高」（理由是它每次只写 67/128 个 Path），
**这个预测是错的，实际恰好相反**。

**结论：不能用单一阈值适配所有工作点。** 阈值选型必须在目标工作点上实测。

### 三个手段的开销来源不同

```
   总 CIR 写入 = 提交次数 × 每次写的寄存器个数
                   ▲            ▲
              放长间隔压这个   阈值过滤压这个

   压力读取（layer_once）是另一条完全独立的路径，只读不写
```

毫秒级控制的完整实验规格见 `TASK_CIR_WRITE_THRESHOLD.md`。
